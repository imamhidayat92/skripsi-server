/**
* ComponentLocator function is to bind every component with it's data. It receive component location map with it's
* corresponding configuration.
*/
function ComponentLocator(config) {
   this._config = config;
   this._init();
}

ComponentLocator.prototype._init = function() {
   
};
