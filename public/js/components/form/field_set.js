var FieldSet = React.createClass({
   render: function() {
      
   }
});

var FieldSetComponent = function(containerId, fields) {
   React.render(
      <FieldSet />,
      document.getElementById(containerId)
   );
};
