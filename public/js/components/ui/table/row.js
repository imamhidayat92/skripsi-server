var Row = React.createClass({
   render: function() {
      var cells = this.props.cells;
      return(
         <tr>{cells.map(function(cell) { return  })}</tr>
      );
   }
});

var RowComponent = function(containerId, cells) {

};
